# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.  Line
# up the first '|' above the ':' following the base package name, and the '|'
# on the right side marks the last column you can put a character in.  You must
# make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

             |-----handy-ruler------------------------------------------------------|
unreal-engine: unreal-engine (Epic Games Unreal Engine 5 3D creation suite)
unreal-engine:
unreal-engine: Unreal Engine is an advanced real-time 3D creation tool delivering
unreal-engine: high-fidelity photorealism, dynamic physics, Nanite virtualized
unreal-engine: geometry, Lumen global illumination, and Niagara VFX.
unreal-engine:
unreal-engine: This package integrates 3Dconnexion SpaceMouse 6-DoF navigation,
unreal-engine: NVIDIA CUDA, AMD ROCm OpenCL compute, and Vulkan SM6 acceleration.
unreal-engine: Supports any user-provided Unreal Engine 5.x Linux release.
unreal-engine:
unreal-engine: Homepage: https://www.unrealengine.com/
